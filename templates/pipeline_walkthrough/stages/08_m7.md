# 08 M7 Custom Component Assembler

What M7 reads:
- target: `../shared/optimization_target/m_minus1_target.json`
- execution plan: `../shared/optimizer_outputs/m5_execution_plan.json`

What M7 produces:
- `../shared/optimizer_outputs/reassembly/`

Why it exists:
- packages the plan into a new Home Assistant custom component instead of patching original integrations
